Bundled origin/main base (v3 harness, unchanged through v5)
==========================================================

v2 independent review could not re-run 51 tests because the
receiving environment lacked git object
25a3f88b8aab61a5462c06efa133b21865ef6083.

v3 includes the test-required origin/main tree as:

  base/origin_main_25a3f88.tar.gz
  base/IDENTITY.txt
  base/SHA256SUMS.txt
  base/GIT_LS_TREE.txt

IDENTITY records:
  BASE_COMMIT
  ROOT_TREE_SHA
  WIN5_TREE_SHA
  TAR_SHA256
  FILE_COUNT=2565
  BASE_SCOPE=full-tree

The tar SHA256 is unchanged from v3/v4:
  d1e6b5847f1387d696d39b87ad0dbfa4e44f8322ad1caecb75ce0969bad5edff

run_tests.py verifies those SHAs, extracts the tar to a temporary
directory, overlays files/, checks the WAL init contract, runs
unittest (51 tests twice plus concurrency x20), and asserts OK.
It does not read tests/*.log as a pass condition.
It does not SSH, deploy, or POST.

This base tree is not in 01_docs/deploy_files.txt.
Do not deploy base/ to Production.
