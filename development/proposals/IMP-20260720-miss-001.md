# Improvement Proposal — IMP-20260720-miss-001

> **コード生成禁止。** Prediction Core 変更は Canary 通過後のみ。  
> Schema: `expect-improvement-proposal/1.0`

| 項目 | 値 |
|------|-----|
| proposal_id | `IMP-20260720-miss-001` |
| status | `ready_for_canary` |
| event_types | `miss` |
| fingerprints | `M-T1-NEAR`, `M-CONF-HIGH` |
| created_at | 2026-07-20T11:35:00Z |

---

## 1. 目的（purpose）

`miss_top1`（勝者は候補プール内だが Top1 外）の比率を下げ、本命選定の近傍精度を改善する設計を確定する。実 Production Evidence が空のため、仮説検証可能な Canary 計画までを成果とする。

## 2. 対象（target）

- **対象:** Top1 選定ロジックの設計方針（ランキング較正・近傍ミスの扱い）
- **非対象（本 Proposal）:** Feature ETL、Result Sync、OPS-Monitor
- **適用条件:** `feature_missing` 非併発の miss クラスタのみ（併発は Feature Proposal 優先）

## 3. 期待効果（expected_effect）

- オフライン Canary 期間において、ベースライン比で `miss_top1` 件数または率が非悪化、かつ可能なら低減
- `hit_at_1` 率がベースライン以上
- 高 confidence（≥80）帯の miss 比率が悪化しない

## 4. 副作用（side_effects）

- Top1 を保守的にすると explain / 自信表現が弱く見える可能性
- Top3 内での並び替えにより `miss_top3` 分布が変わる可能性
- Feature 欠落下で適用すると見かけの改善がデータ問題を隠すリスク → **適用ゲートで除外**

## 5. 評価方法（evaluation_method）

1. `evidence/improvement/miss` を日付固定でベースライン化（未同期時は GameDay fixture で代用し、実データ到着後に差し替え）
2. Canary Config / Criteria（本 ID）に従いオフライン評価
3. ゲート: `no_hit_at_1_regression`, `no_miss_top1_increase`, `no_feature_cooccurrence_worsening`
4. PASS 後にのみ Core 変更の実装・PR を許可

---

## 非目標（non_goals）

- Production 上での即時 Core 変更
- feature_missing 併発レースへの適用
- コード自動生成

## Evidence / Analysis 参照

- analysis: `development/analysis/miss/2026-07-20-trend-baseline.md`
- index: `development/index/by-event-type/summary.json`（corpus empty）

## Canary への引き継ぎ

- Config: `development/canary/configs/IMP-20260720-miss-001.json`
- Criteria: `development/canary/criteria/IMP-20260720-miss-001.json`
- Report: `development/canary/reports/IMP-20260720-miss-001.json`（pending）
