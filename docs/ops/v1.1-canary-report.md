# Version 1.1 Canary Report — UI/Ops RC

**Date:** 2026-07-21 (JST)  
**Branch / RC:** `feature/v1.1-ui-ops` @ `238fd28`  
**Environment:** Local `wrangler pages dev` → `http://127.0.0.1:8788`  
**Baseline Freeze:** `v1.0.0-stable`（未変更）  
**判定:** **GO（Canary PASS）**

証拠ディレクトリ: [`canary-v1.1-evidence/`](./canary-v1.1-evidence/)  
設計: [`v1.1-auto-maintenance-design.md`](./v1.1-auto-maintenance-design.md)  
Harness: `scripts/ops/canary-v1.1-auto-maint.mjs`

---

## 総合判定

| 判定 | 理由 |
|------|------|
| **GO** | Flag OFF 既定で現行互換。対象 Flag の個別 ON 表示・API 互換・Auto Maintenance（平日 CLOSED / 手動優先 / Flag OFF ロールバック）を確認。致命的 JS Error なし。契約テスト 133 PASS、Auto Maintenance 契約 9 PASS、Harness 11 PASS |

**Hold にする条件は検出されず。** 下記「注意・運用メモ」は merge ブロッカーではない。

---

## 1. Flag OFF（既定）

| 項目 | 結果 | 証拠 |
|------|------|------|
| 既定値 | `v11_auto_maintenance` ほか Canary 対象 Flag は **false**（`v11_loading_errors` のみ true） | `public/config/beta.json` / harness |
| Version 1.0 互換 | `/api/ops/public-status` → `ops_mode=PUBLIC`, `reason=default_public`, `auto_maintenance_enabled=false`（平日でも自動 CLOSED しない） | API probe |
| UI 差分 | 自動メンテ OFF 時 `maintenance.html` は index へ退避（Flag OFF パス） | Gate 実装 + 手動確認 |
| JS Error | Console hook で **エラー 0**（login / races） | CDP `window.__canaryErrors = []` |
| 契約 | `npm run test:contract` **133 / 133 PASS** | CLI |

---

## 2. 各 Feature Flag 個別 / まとめて ON

ローカル Canary で UI 系 Flag を一時 ON（終了時に **false へ復元済み**）。

| Flag | 表示 | API 互換 | モバイル | Console |
|------|------|----------|----------|---------|
| UI Improvements（mobile/home/races/race_detail） | PASS（一覧・フィルタ・カード） | PASS（predictions 200） | PASS（viewport） | 0 errors |
| Explain | PASS（fallback バッジ等・既存 bundle 経路） | 破壊なし | PASS | 0 |
| Confidence | PASS（AI信頼度 % 表示） | 破壊なし | PASS | 0 |
| Collector Status | PASS（`Collector: HOLD（Real 未接続）`） | Real KeibaNet **HOLD 維持** | PASS | 0 |
| System Health | PASS（`BFF OK · …ms`） | `/api/health` 200 | PASS | 0 |
| Ops Dashboard | PASS（HEALTH / latency / mock_fallback / COLLECTOR HOLD） | 新規サーバ metrics API なし（設計どおり） | PASS | 0 |

スクリーンショット:

- [`canary-v1.1-evidence/02-ops-dashboard.png`](./canary-v1.1-evidence/02-ops-dashboard.png)
- [`canary-v1.1-evidence/03-races-ui-flags-on.png`](./canary-v1.1-evidence/03-races-ui-flags-on.png)

---

## 3. Auto Maintenance

実施日は **2026-07-21（火）JST** → WeekendProvider 上は非開催日。

| シナリオ | 結果 |
|----------|------|
| Flag ON + 平日 | `ops_mode=CLOSED`, `reason=auto_calendar` |
| `maintenance.html` | メンテ文言・次回公開 `2026-07-25（土）`・招待案内・「会話型AIは開催日に利用できます」 |
| USER `/api/predictions` | **503 `OPS_CLOSED`** |
| USER `/api/conversation/chat` | **503 `OPS_CLOSED`** |
| ADMIN `/api/predictions` | **200**（bypass） |
| Health / public-status | **200**（exempt） |
| Flag ON + 土日 | Harness / 契約テストで `PUBLIC`（当日は平日のためライブ土日はユニットで担保） |

スクショ: [`01-maintenance-weekday.png`](./canary-v1.1-evidence/01-maintenance-weekday.png)

---

## 4. 手動優先順位

| 設定 | 期待 | 実測 |
|------|------|------|
| `ops_mode: PUBLIC` + Flag ON + 平日 | 強制公開 | `PUBLIC` / `manual_ops_mode` / USER predictions **200** |
| `maintenance_mode: true` + Flag ON | 強制 CLOSED | `CLOSED` / `manual_maintenance_mode` / USER **503** |
| `ops_mode: CLOSED` | 土日でも CLOSED | Harness PASS |

優先順位は仕様どおり: **手動 ops_mode / maintenance_mode → 自動開催日 → PUBLIC**

---

## 5. Rollback

| 手順 | 結果 |
|------|------|
| `v11_auto_maintenance: false` のみ | `auto_maintenance_enabled=false`, `ops_mode=PUBLIC`, USER predictions **200**（平日） |

**Flag OFF のみで即時復旧できることを確認。**

運用メモ: BFF `getBetaConfig` はプロセス内キャッシュ。ASSETS だけ差し替える場合は **Pages 再デプロイ（または Worker 再起動）** が必要（Rollback 手順の「再デプロイ」と一致）。

---

## Console Error

| 画面 | Errors |
|------|--------|
| login | 0 |
| maintenance | 0（hook 空） |
| races（Flag ON） | 0 |

---

## Lighthouse（local headless）

| ページ | Performance | Accessibility | Best Practices |
|--------|--------------|---------------|----------------|
| `login.html` | **63** | **98** | **100** |
| `maintenance.html` | **65** | **98** | **100** |

JSON: `canary-v1.1-evidence/lighthouse-*.json`  
注: Windows 上で ChromeLauncher 一時ディレクトリ削除時に EPERM が出るが、スコア JSON は取得済み。ローカル mock / 未最適化アセットのため Performance は参考値。a11y / BP は問題なし。

---

## 非対象の確認

| 項目 | 状態 |
|------|------|
| Prediction Core | 変更なし |
| RePick | Flag OFF / research のみ |
| Real KeibaNet | HOLD（UI 表示で確認） |
| `v1.0.0-stable` | 未変更 |
| main merge | **未実施**（本 Canary 後の Merge Plan 参照） |

---

## Go / Hold

### **GO — Canary PASS**

Merge 準備に進んでよい。段階 Canary（本番 Flag は既定 false のまま merge → ステージングで個別 ON）を推奨。
