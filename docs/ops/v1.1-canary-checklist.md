# Version 1.1 — Canary / リリース準備メモ

**Branch:** `feature/v1.1-ui-ops`  
**Freeze:** `v1.0.0-stable` は変更しない。本番 tag は将来 `v1.1.0`。

## Flag Canary 手順

1. `public/config/beta.json` / `config/beta.json` の `ui_features` を段階 ON  
2. 既定: `v11_loading_errors=true`、他 `false`  
3. 推奨 Canary 順: mobile → home → races → race_detail → explain → confidence → collector_status → system_health → ops_dashboard  
4. 各段階で: ログイン → ホーム → 一覧 → 詳細、および **全 Flag OFF** での見た目回帰

## 回帰チェック

- [ ] Flag 全 OFF（loading 除く）で主要画面が v1.0 相当
- [ ] 招待ログイン / setup 非破壊
- [ ] `/api/predictions` 契約非破壊
- [ ] Prediction Core / RePick / Real KeibaNet 未変更
- [ ] `ops.html` が一般 nav に出ない
- [ ] Assets `?v=11` でキャッシュ更新

## 禁止

- `v1.0.0-stable` タグの移動
- `WIN5_REPICK_V2_ENABLED` ON
- Real KeibaNet 本接続
