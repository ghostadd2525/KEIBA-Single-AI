# Optional third-party deps.
# Current runtime is Python 3.11+ stdlib only (ThreadingHTTPServer).
# No FastAPI/uvicorn required for v1.0.0-beta AI host.
#
# Install (empty set is OK):
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
