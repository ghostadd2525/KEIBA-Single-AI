# Optional third-party deps for AI Core on the same host.
# HTTP service itself remains stdlib-only.
#
#   python3 -m pip install -r requirements.txt
#
# pandas/numpy are required when AI_ENGINE=real and ai_platform is linked.
pandas>=2.0
numpy>=1.24
