# Improvement Evidence (canonical for Cursor / Development)

event_type subdirs: miss | feature_missing | prediction_failed | result_sync_failed

manifest/YYYY-MM-DD/{run,summary,index}.json

Legacy dual-write: ../miss/ (compat). Prefer this tree.
