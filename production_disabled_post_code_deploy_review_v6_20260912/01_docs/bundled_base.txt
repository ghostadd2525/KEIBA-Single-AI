Bundled origin/main base (v3 harness, unchanged through v6)
==========================================================

v2 independent review could not re-run tests because the
receiving environment lacked git object
25a3f88b8aab61a5462c06efa133b21865ef6083.

v3 includes the test-required origin/main tree as:

  base/origin_main_25a3f88.tar.gz
  base/IDENTITY.txt
  base/SHA256SUMS.txt
  base/GIT_LS_TREE.txt

The tar SHA256 is unchanged from v3/v4/v5:
  d1e6b5847f1387d696d39b87ad0dbfa4e44f8322ad1caecb75ce0969bad5edff

run_tests.py verifies those SHAs, extracts the tar, overlays files/,
asserts migrate() and _connect_idempotent() do not write journal_mode,
runs 52 tests twice plus DELETE-journal concurrency x20.
It does not read tests/*.log as a pass condition.
It does not SSH, deploy, POST, or run live-compare.

This base tree is not in 01_docs/deploy_files.txt.
Do not deploy base/ to Production.
